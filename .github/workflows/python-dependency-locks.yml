name: Python Dependency Locks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-locks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        # v5
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Install lock tooling
        uses: validityBase/vbase-github-actions/.github/actions/setup-python-deps@v1
        with:
          requirements-files: |
            requirements/tools.txt
          python-version: "3.12"
          require-hashes: "true"

      - name: Verify generated lock files are current
        run: |
          pip-compile --strip-extras --no-annotate --generate-hashes -o requirements/docs.txt requirements/docs.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements/e2e.txt requirements/e2e.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements/tools.txt requirements/tools.in
          git diff --exit-code -- requirements/docs.txt requirements/e2e.txt requirements/tools.txt

      - name: Install docs dependencies
        uses: validityBase/vbase-github-actions/.github/actions/setup-python-deps@v1
        with:
          requirements-files: |
            requirements/docs.txt
          python-version: "3.12"
          require-hashes: "true"

      - name: Check installed dependencies
        run: python -m pip check
